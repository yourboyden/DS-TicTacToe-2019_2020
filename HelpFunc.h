// Helpful functions, libraries, etc.

#pragma once
#pragma warning(disable : 4996) 
#include <iostream>
#include <fstream>
#include <time.h>
using namespace std;

const int NEG_CAP = -1000;
const int POS_CAP = 1000;
const int STRING_CAP = 1025;
int generateRandom();
int max(const int, const int);
int min(const int, const int);